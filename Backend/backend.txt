============== User ======================

- Create Account
    - Validate the input data.
    - Verify if email already exist.
    - Hash the user password.

x Login
    x Validate input data.
    x Search for a user.
    x Compare passwords.
    x Generate a token.