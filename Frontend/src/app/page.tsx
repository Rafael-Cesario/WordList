import { Header } from "@/features/home/header";
import { ListContainer } from "@/features/home/listContainer";

const Home = () => {
	return (
		<>
			<Header />
			<ListContainer />
		</>
	);
};

export default Home;
