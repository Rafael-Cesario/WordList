Save in the params the index of the group.
Save in the sessionStorage the listGlobalState.

IndexOfFirstWord = groupIndex * wordsPerWordlist
RealIndexOfCurrentWord = ( groupIndex * wordsPerWordList ) + IndexWord
groupWords = allTheWords.slice(indexOfFirstWord, WordsPerWordList)
